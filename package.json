{
  "name": "less-is-more",
  "private": true,
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --gc --minify"
  },
  "devDependencies": {
    "postcss": "^8.4.47",
    "postcss-cli": "^11.0.0",
    "autoprefixer": "^10.4.20"
  }
}
